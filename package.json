{
	"name": "fantasy-monorepo",
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"frontend:build": "bun run --cwd packages/game build",
		"backend:start": "bun run --cwd packages/server index.ts"		
	}
}
